<Window x:Class="UI.ProjetNETCora.Views.Operation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Operation" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="35"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="0" HorizontalAlignment="Stretch" Background="AliceBlue" FontWeight="Bold">AJOUTER STOCK</Label>

        <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Orientation="Vertical">
            <Label>Code du produit</Label>
            <TextBox Text="{Binding Code}"></TextBox>
            <Label>Stock du produit</Label>
            <TextBox Text="{Binding Stock}"></TextBox>
        </StackPanel>

        <Button Grid.Column="3" Margin="5" Grid.Row="2" FontSize="12px" Command="{Binding AjouterStock}">Ajouter stock</Button>
    </Grid>
</Window>